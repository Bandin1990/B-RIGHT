# B-RIGHT System Specification

เอกสารออกแบบระบบรุ่นเริ่มต้นสำหรับระบบช่วยประเมินและจัดทำความเห็นการปล่อยชั่วคราวในชั้นสอบสวน

สถานะ: Draft 0.1  
อ้างอิงข้อกำหนด: `B-RIGHT_Rules_and_Data_Requirements.md`

## 1. ขอบเขต MVP

MVP จะรองรับกระบวนการตั้งแต่รับตัวผู้ต้องหา จนถึงสร้างเอกสารเสนอผู้บังคับบัญชา โดยยังไม่เชื่อมฐานข้อมูลจริงจนกว่าจะได้รับสิทธิ์และรายละเอียด API จากหน่วยงานเจ้าของข้อมูล

### อยู่ใน MVP

- ระบบเข้าสู่ระบบและกำหนดสิทธิ์
- จัดการคดีและผู้ต้องหา
- บันทึกการแจ้งสิทธิ
- กรอกข้อหาและพฤติการณ์
- แบบประเมินความเสี่ยง
- จำลองข้อมูลจากหน่วยงานภายนอก
- ประเมินผู้ค้ำและหลักทรัพย์เบื้องต้น
- สร้างความเห็นพนักงานสอบสวน
- เสนอและส่งต่อผู้บังคับบัญชา
- สร้างเอกสาร PDF
- Audit log
- แจ้งเตือนกรอบเวลา 48 ชั่วโมง

### ยังไม่อยู่ใน MVP

- การดึงข้อมูลอาชญากรรมจริง
- การดึงข้อมูลทรัพย์สินจริง
- การลงนามอิเล็กทรอนิกส์ที่มีผลทางราชการ
- การอนุมัติแทนผู้มีอำนาจ
- การกำหนดสูตรวงเงินที่ยังไม่ได้รับรอง

## 2. บทบาทผู้ใช้งาน

| บทบาท | สิทธิ์หลัก |
|---|---|
| พนักงานสอบสวน | สร้างคดี กรอกข้อมูล ขอข้อมูลภายนอก ทำแบบประเมิน และเสนอความเห็น |
| ผู้บังคับบัญชา | ตรวจสอบข้อมูล เห็นคำแนะนำ แก้ไขความเห็น อนุมัติ/ไม่อนุมัติ/ส่งกลับ |
| เจ้าหน้าที่ตรวจหลักประกัน | ตรวจเอกสารทรัพย์สิน ตรวจผู้ค้ำ และบันทึกผลการตรวจ |
| ผู้ดูแลกฎ | จัดการข้อหา คำถาม น้ำหนักคะแนน สูตร และเวอร์ชันกฎ |
| ผู้ดูแลระบบ | จัดการผู้ใช้ หน่วยงาน สิทธิ์ และการเชื่อมต่อ |
| ผู้บริหาร | ดูสถิติภาพรวมแบบลดทอนข้อมูลส่วนบุคคล |

## 3. โมดูลระบบ

### 3.1 Case Management

หน้าที่:

- สร้างและค้นหาคดี
- ลงทะเบียนผู้ต้องหา
- บันทึกเวลาเริ่มควบคุมตัว
- แสดงสถานะ workflow
- แสดงรายการเอกสารและเหตุการณ์ทั้งหมด

สถานะคดีที่แนะนำ:

```text
ร่าง → รอตรวจข้อมูล → ประเมินแล้ว → รอความเห็น → รออนุมัติ
→ อนุมัติปล่อย → ไม่อนุมัติ → ปิดคดี
```

### 3.2 External Data Gateway

เป็นชั้นกลางสำหรับข้อมูลภายนอก ไม่ให้ frontend เรียกหน่วยงานภายนอกโดยตรง

คุณสมบัติ:

- adapter แยกตามแหล่งข้อมูล
- ตรวจสิทธิ์ก่อนร้องขอ
- บันทึก request/response metadata
- masking ข้อมูลที่ไม่จำเป็น
- รองรับข้อมูลสดและข้อมูลจากเอกสารแนบ
- ระบุ `source`, `retrieved_at`, `verified_by`, `confidence`

### 3.3 Legal Rules Engine

แยกกฎออกจากโค้ดโปรแกรม เพื่อให้แก้ไขตามกฎหมายหรือระเบียบได้

องค์ประกอบ:

- ฐานข้อหาและบทกฎหมาย
- ปัจจัยประเมินความเสี่ยง
- น้ำหนักคะแนน
- เงื่อนไขเส้นทางการปล่อย
- เงื่อนไขหลักประกัน
- สูตรคำนวณมูลค่าทรัพย์สิน
- ตารางเอกสารที่ต้องใช้
- วันเริ่มใช้และวันสิ้นสุดของกฎ

ทุกผลลัพธ์ต้องเก็บ `rule_version` ไว้กับการประเมินเสมอ

### 3.4 Risk Assessment

หน้าจอแบ่งเป็นกลุ่ม:

- ความเสี่ยงหลบหนี
- ความเสี่ยงยุ่งเหยิงพยานหรือหลักฐาน
- ความเสี่ยงก่ออันตราย
- ความร้ายแรงและพฤติการณ์แห่งคดี
- ความผูกพันกับพื้นที่ ครอบครัว และอาชีพ
- ประวัติการมาตามนัดและการผิดเงื่อนไข

ระบบต้องแสดงทั้งคะแนนรวมและเหตุผลรายข้อ ไม่แสดงเพียงสีเขียว เหลือง หรือแดงโดยไม่มีคำอธิบาย

### 3.5 Guarantee and Asset Assessment

รองรับ 2 ประเภท:

1. ผู้ค้ำ/บุคคลผู้รับรอง
2. ทรัพย์สินหรือหลักทรัพย์

ผลการตรวจแบ่งเป็น:

```text
ผ่านเบื้องต้น
ต้องตรวจเพิ่ม
ข้อมูลไม่เพียงพอ
ไม่ผ่านตามกฎที่ใช้งาน
```

สำหรับทรัพย์สิน ระบบต้องแสดงมูลค่าต้นทาง มูลค่าหลังหักภาระผูกพัน และมูลค่าที่เสนอเบื้องต้นแยกจากกัน

### 3.6 Approval Workflow

- พนักงานสอบสวนส่งความเห็น
- ผู้บังคับบัญชาตรวจสอบ
- อนุมัติแนวทางหนึ่ง
- ไม่อนุมัติพร้อมเหตุผล
- ส่งกลับเพื่อแก้ไขหรือขอข้อมูลเพิ่ม
- ลงวันที่และชื่อผู้มีคำสั่ง

ระบบต้องไม่เปลี่ยนความเห็นของพนักงานสอบสวนเมื่อผู้บังคับบัญชามีคำสั่งต่างกัน แต่ต้องเก็บทั้งสองรายการไว้เป็นประวัติ

### 3.7 Document Generator

เอกสารทุกฉบับต้องสร้างจาก template ที่กำหนดเวอร์ชันได้ และประกอบด้วย:

- ข้อมูลคดีและผู้ต้องหา
- ผลประเมิน
- ข้อมูลภายนอกพร้อมวันที่ดึง
- รายการหลักประกัน
- ความเห็นพนักงานสอบสวน
- ความเห็น/คำสั่งผู้บังคับบัญชา
- ช่องลงนาม
- เลขเอกสารและ QR ตรวจสอบเอกสาร

## 4. โครงสร้างข้อมูลหลัก

```text
users
organizations
roles
cases
suspects
charges
rights_acknowledgements
external_data_requests
external_data_snapshots
risk_assessments
risk_factors
guarantors
assets
asset_valuations
guarantee_options
officer_opinions
approval_decisions
documents
notifications
audit_logs
rule_versions
```

### ความสัมพันธ์หลัก

```text
case 1 ── n suspect
case 1 ── n charge
case 1 ── n external_data_request
case 1 ── n risk_assessment
case 1 ── n guarantee_option
case 1 ── n document
case 1 ── n audit_log
risk_assessment 1 ── n risk_factor
guarantee_option 1 ── 0..1 guarantor
guarantee_option 1 ── 0..n asset
```

## 5. API ระดับต้นแบบ

```text
POST   /api/cases
GET    /api/cases
GET    /api/cases/:id
PATCH  /api/cases/:id

POST   /api/cases/:id/rights
POST   /api/cases/:id/external-data/requests
GET    /api/cases/:id/external-data

POST   /api/cases/:id/risk-assessments
GET    /api/cases/:id/risk-assessments/latest
POST   /api/cases/:id/guarantees
POST   /api/cases/:id/assets/valuate

POST   /api/cases/:id/opinion
POST   /api/cases/:id/submit
POST   /api/cases/:id/decision

POST   /api/cases/:id/documents/generate
GET    /api/documents/:id/download
GET    /api/cases/:id/audit-log
```

## 6. หน้าจอ MVP

1. Login
2. Dashboard งานของฉัน
3. รายการคดี
4. สร้างคดี
5. รายละเอียดคดี
6. ข้อมูลผู้ต้องหาและข้อหา
7. แจ้งสิทธิ
8. ข้อมูลภายนอก
9. แบบประเมินความเสี่ยง
10. ผู้ค้ำและหลักทรัพย์
11. สรุปคำแนะนำ
12. ความเห็นพนักงานสอบสวน
13. หน้าตรวจอนุมัติของผู้บังคับบัญชา
14. เอกสารและประวัติการดำเนินการ

## 7. Acceptance Criteria ของ MVP

- ผู้ใช้แต่ละบทบาทเห็นเฉพาะเมนูและข้อมูลที่ได้รับอนุญาต
- สร้างคดีและบันทึกเวลาเริ่มควบคุมตัวได้
- กรอกข้อหาและปัจจัยเสี่ยงได้ครบ พร้อมเหตุผลรายข้อ
- ระบบสร้างคำแนะนำจากกฎเวอร์ชันที่ระบุได้
- ระบบแสดงข้อมูลจำลองภายนอกพร้อมแหล่งที่มาและวันเวลา
- คำนวณมูลค่าทรัพย์สินเบื้องต้นโดยแยกมูลค่าต้นทางและมูลค่าที่คำนวณ
- พนักงานสอบสวนเขียนความเห็นเพิ่มเติมได้
- ผู้บังคับบัญชาสั่งอนุมัติ ไม่อนุมัติ หรือส่งกลับได้
- สร้าง PDF เสนออนุมัติได้
- ทุกการแก้ไขสำคัญปรากฏใน audit log
- ระบบเตือนคดีใกล้ครบกรอบเวลา
- ระบบไม่สามารถอนุมัติแทนมนุษย์โดยอัตโนมัติ

## 8. สถาปัตยกรรมที่แนะนำ

```text
Next.js / TypeScript
        ↓
Backend API + Auth + RBAC
        ↓
PostgreSQL ─ Document Storage ─ Audit Store
        ↓
External Data Gateway
        ├─ Criminal Data Adapter
        ├─ Government Platform Adapter
        ├─ Asset Data Adapter
        └─ Justice Fund Adapter
```

ระยะแรกควรใช้ข้อมูลจำลองและ adapter แบบ mock เพื่อทดสอบ workflow ให้ครบก่อนเชื่อมข้อมูลจริง

## 9. ลำดับการพัฒนา

### Sprint 1: โครงสร้างระบบ

- ตั้งค่าโปรเจกต์
- Login/RBAC
- โครงสร้างฐานข้อมูล
- Layout และ navigation

### Sprint 2: คดีและผู้ต้องหา

- สร้างคดี
- ข้อมูลผู้ต้องหา
- ข้อหา
- ตัวจับเวลา 48 ชั่วโมง

### Sprint 3: ประเมินความเสี่ยง

- แบบประเมิน
- Rules Engine รุ่น mock
- ผลลัพธ์ 3 ระดับ
- เหตุผลและ audit log

### Sprint 4: หลักประกันและเอกสาร

- ผู้ค้ำ
- ทรัพย์สินจำลอง
- คำนวณมูลค่าเบื้องต้น
- สร้างเอกสาร PDF

### Sprint 5: เสนออนุมัติ

- ส่งความเห็น
- หน้าผู้บังคับบัญชา
- อนุมัติ/ไม่อนุมัติ/ส่งกลับ
- Dashboard และแจ้งเตือน

### Sprint 6: เตรียมเชื่อมข้อมูลจริง

- สรุป API และสิทธิ์ข้อมูลของแต่ละหน่วยงาน
- กำหนด MOU และ data sharing agreement
- สร้าง adapter แยกตามแหล่งข้อมูล
- ทดสอบความปลอดภัยและการปกปิดข้อมูล

## 10. ความเสี่ยงที่ต้องควบคุม

- ข้อมูลภายนอกไม่ตรงกันหรือไม่เป็นปัจจุบัน
- กฎหรือระเบียบมีการแก้ไข
- ข้อมูลอาชญากรรมมีความอ่อนไหวสูง
- ราคาประเมินทรัพย์สินไม่เท่ากับมูลค่าบังคับขายจริง
- การใช้คะแนนอัตโนมัติอาจสร้างอคติ
- ผู้ใช้เข้าใจคำแนะนำเป็นคำสั่ง

จึงต้องแสดงคำเตือนทุกหน้าผลลัพธ์ว่าเป็นคำแนะนำเบื้องต้น และบังคับให้มีการยืนยันโดยเจ้าหน้าที่ก่อนส่งต่อ
